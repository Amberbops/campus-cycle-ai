# Core framework (FastAPI used locally; Lambda uses the handler directly)
fastapi>=0.111.0
uvicorn[standard]>=0.30.1
mangum>=0.17.0          # ASGI adapter for Lambda

# AWS SDK
boto3>=1.34.131
botocore>=1.34.131

# Strands Agents SDK
strands-agents>=0.1.5
strands-agents-tools>=0.1.5

# Pydantic for schema validation
pydantic>=2.7.4
pydantic-settings>=2.5.2

# Utilities
python-dotenv>=1.0.1
pillow>=11.2.1
httpx>=0.27.1            # Async HTTP client (compatible with mcp)
ulid-py>=1.1.0           # Sortable unique IDs

# Observability
aws-lambda-powertools>=2.40.1

# Testing
pytest>=8.2.2
pytest-asyncio>=0.23.7
moto[s3,dynamodb]>=5.0.9
